# 快捷按钮功能实现计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在弹窗对话区域的输入框上方增加快捷按钮，实现点击即发送的快速回复功能。

**Architecture:** 创建独立的快捷按钮识别模块 `quick-buttons.ts`，在主组件中渲染按钮区域，通过智能识别最后一条 AI 消息动态生成方案按钮。

**Tech Stack:** Lit (Web Components), TypeScript, CSS-in-JS

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `src/lib/quick-buttons.ts` | 创建 | 快捷按钮识别逻辑 |
| `src/styles.ts` | 修改 | 添加快捷按钮样式 |
| `src/kanban-watcher-card.ts` | 修改 | 渲染按钮和处理点击事件 |

---

## Chunk 1: 快捷按钮识别模块

### Task 1: 创建快捷按钮识别模块

**Files:**
- Create: `src/lib/quick-buttons.ts`

- [ ] **Step 1: 创建 quick-buttons.ts 文件**

```typescript
// src/lib/quick-buttons.ts

/** 通用快捷词（始终显示） */
export const STATIC_BUTTONS = ["继续", "同意"] as const;

/** 动态按钮识别正则规则 */
const DYNAMIC_BUTTON_PATTERNS = [
  // 中文数字方案: 方案一、方案二
  /方案([一二三四五六七八九十]+)/g,
  // 阿拉伯数字方案: 方案1、方案 2
  /方案\s*([1-9])/g,
  // 中文数字选项: 选项一、选项二
  /选项([一二三四五六七八九十]+)/g,
  // 字母选项: 选项A、选项 B
  /选项\s*([A-Za-z])/g,
  // 英文选项: Option A、OptionB
  /Option\s*([A-Za-z])/gi,
  // 序号列表: 1.、2.、3.（行首）
  /^\s*([1-9])\./gm,
  // 括号字母: (A)、(B)、(C)
  /\(([A-Za-z])\)/g,
];

/** 中文数字到阿拉伯数字的映射 */
const CHINESE_NUMBER_MAP: Record<string, string> = {
  一: "1",
  二: "2",
  三: "3",
  四: "4",
  五: "5",
  六: "6",
  七: "7",
  八: "8",
  九: "9",
  十: "10",
};

/** 最大动态按钮数量 */
const MAX_DYNAMIC_BUTTONS = 5;

/**
 * 从文本中提取动态按钮
 * @param text AI 消息文本
 * @returns 动态按钮列表（已去重，最多5个）
 */
export function extractDynamicButtons(text: string): string[] {
  if (!text || typeof text !== "string") {
    return [];
  }

  const matches = new Set<string>();

  for (const pattern of DYNAMIC_BUTTON_PATTERNS) {
    // 重置正则的 lastIndex
    pattern.lastIndex = 0;

    let match: RegExpExecArray | null;
    while ((match = pattern.exec(text)) !== null) {
      const captured = match[1];
      if (captured) {
        // 处理不同格式的匹配结果
        const buttonText = normalizeButtonText(match[0], captured);
        if (buttonText) {
          matches.add(buttonText);
        }
      }
    }
  }

  // 转为数组，限制数量
  return Array.from(matches).slice(0, MAX_DYNAMIC_BUTTONS);
}

/**
 * 标准化按钮文本
 * @param fullMatch 完整匹配文本
 * @param captured 捕获组内容
 * @returns 标准化后的按钮文本
 */
function normalizeButtonText(fullMatch: string, captured: string): string {
  // 中文数字转阿拉伯数字
  if (CHINESE_NUMBER_MAP[captured]) {
    // 方案一 → 方案1
    return fullMatch.replace(captured, CHINESE_NUMBER_MAP[captured]);
  }

  // 字母统一大写
  if (/^[A-Za-z]$/.test(captured)) {
    // 选项a → 选项A
    return fullMatch.replace(captured, captured.toUpperCase());
  }

  // 其他情况保持原样
  return fullMatch.trim();
}

/**
 * 检查按钮文本是否为有效选项
 * @param text 按钮文本
 * @returns 是否有效
 */
export function isValidButtonText(text: string): boolean {
  return typeof text === "string" && text.trim().length > 0 && text.trim().length <= 20;
}
```

- [ ] **Step 2: 提交快捷按钮识别模块**

```bash
git add src/lib/quick-buttons.ts
git commit -m "feat: 添加快捷按钮识别模块"
```

---

## Chunk 2: 样式实现

### Task 2: 添加快捷按钮样式

**Files:**
- Modify: `src/styles.ts:571-575` (在 `.dialog-composer` 之前)

- [ ] **Step 1: 在 styles.ts 中添加快捷按钮样式**

在 `.message-bubble.is-ai` 样式块之后，`.dialog-composer` 样式块之前添加：

```typescript
  /* 快捷按钮区域 */
  .quick-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    padding: 4px 0;
  }

  .quick-button {
    padding: 5px 10px;
    border-radius: 8px;
    font: inherit;
    font-size: 0.82rem;
    font-weight: 600;
    cursor: pointer;
    transition: transform 120ms ease, opacity 120ms ease;
  }

  .quick-button:active {
    transform: scale(0.96);
  }

  .quick-button.is-static {
    border: 0;
    background: var(--primary-color, #f59e0b);
    color: #ffffff;
  }

  .quick-button.is-dynamic {
    border: 1px solid color-mix(in srgb, var(--divider-color, #cbd5e1) 72%, transparent);
    background: transparent;
    color: inherit;
  }

  .quick-button.is-dynamic:hover {
    background: color-mix(in srgb, var(--secondary-background-color, #f3f4f6) 50%, transparent);
  }
```

- [ ] **Step 2: 添加移动端快捷按钮样式**

在 `@media (max-width: 640px)` 媒体查询内，`.message-input` 样式块之前添加：

```typescript
    /* 快捷按钮移动端样式 */
    .quick-buttons {
      gap: 5px;
    }

    .quick-button {
      padding: 4px 8px;
      font-size: 0.78rem;
    }
```

- [ ] **Step 3: 提交样式更改**

```bash
git add src/styles.ts
git commit -m "style: 添加快捷按钮样式"
```

---

## Chunk 3: 组件集成

### Task 3: 在主组件中集成快捷按钮

**Files:**
- Modify: `src/kanban-watcher-card.ts`

- [ ] **Step 1: 导入快捷按钮模块**

在文件顶部的导入区域添加：

```typescript
import {
  extractDynamicButtons,
  isValidButtonText,
  STATIC_BUTTONS,
} from "./lib/quick-buttons";
```

- [ ] **Step 2: 添加快捷按钮渲染方法**

在 `renderDialog()` 方法之前添加：

```typescript
  private renderQuickButtons(workspace: KanbanWorkspace) {
    const isRunning = workspace.status === "running";
    const messages = this.getDialogMessages(workspace);

    // 获取最后一条 AI 消息
    const lastAiMessage = [...messages].reverse().find(
      (msg) => msg.kind === "message" && msg.sender === "ai"
    );

    // 提取动态按钮
    const dynamicButtons = lastAiMessage && "text" in lastAiMessage
      ? extractDynamicButtons(lastAiMessage.text)
      : [];

    // 合并通用按钮和动态按钮
    const allButtons = [
      ...STATIC_BUTTONS,
      ...(isRunning ? [] : dynamicButtons), // 运行时不显示动态按钮
    ].filter(isValidButtonText);

    if (allButtons.length === 0) {
      return nothing;
    }

    return html`
      <div class="quick-buttons">
        ${allButtons.map((text, index) => {
          const isStatic = index < STATIC_BUTTONS.length;
          return html`
            <button
              class="quick-button ${isStatic ? "is-static" : "is-dynamic"}"
              type="button"
              @click=${() => void this.handleQuickButtonClick(text)}
            >
              ${text}
            </button>
          `;
        })}
      </div>
    `;
  }
```

- [ ] **Step 3: 添加快捷按钮点击处理方法**

在 `handleActionClick` 方法之后添加：

```typescript
  private async handleQuickButtonClick(text: string) {
    const workspace = this.selectedWorkspace;
    if (!workspace) {
      return;
    }

    const isRunning = workspace.status === "running";
    if (isRunning) {
      this.actionFeedback = "工作区正在运行，无法发送快捷消息。";
      return;
    }

    try {
      this.actionFeedback = "正在发送快捷消息...";
      const response = await sendWorkspaceMessage({
        baseUrl: this.config.base_url,
        apiKey: this.config.api_key,
        workspaceId: workspace.id,
        message: text,
      });

      this.appendOptimisticUserMessage(workspace.id, text);
      this.actionFeedback = response.message?.trim()
        ? `发送成功：${response.message.trim()}`
        : "快捷消息已发送";
      this.messageDraft = "";
    } catch (error) {
      this.actionFeedback = this.toErrorMessage(error, "发送快捷消息失败");
    }
  }
```

- [ ] **Step 4: 在弹窗模板中渲染快捷按钮**

修改 `renderDialog()` 方法，在 `.dialog-composer` 元素内、`textarea` 之前添加快捷按钮渲染：

找到以下代码：
```typescript
          <div class="dialog-composer">
            ${isQueued
              ? html`<div class="queue-banner">消息已排队 - 将在当前运行完成时执行</div>`
              : nothing}
            <textarea
```

替换为：
```typescript
          <div class="dialog-composer">
            ${isQueued
              ? html`<div class="queue-banner">消息已排队 - 将在当前运行完成时执行</div>`
              : nothing}
            ${this.renderQuickButtons(workspace)}
            <textarea
```

- [ ] **Step 5: 提交组件集成更改**

```bash
git add src/kanban-watcher-card.ts
git commit -m "feat: 集成快捷按钮到弹窗组件"
```

---

## Chunk 4: 测试验证

### Task 4: 手动测试验证

- [ ] **Step 1: 启动开发服务器**

```bash
npm run dev
```

- [ ] **Step 2: 验证通用按钮**

在浏览器中打开开发服务器地址，点击 workspace 打开弹窗：
- [ ] 确认显示"继续"和"同意"两个通用按钮
- [ ] 点击"继续"按钮，确认消息发送成功
- [ ] 点击"同意"按钮，确认消息发送成功

- [ ] **Step 3: 验证动态按钮识别**

创建包含以下内容的 AI 消息进行测试：
- [ ] "请选择方案一或方案二" → 应显示"方案1"、"方案2"
- [ ] "选项A是X，选项B是Y" → 应显示"选项A"、"选项B"
- [ ] "1. 方案A\n2. 方案B\n3. 方案C" → 应显示"1."、"2."、"3."
- [ ] "你可以选择(A)或(B)" → 应显示"(A)"、"(B)"
- [ ] 普通消息无选项 → 只显示通用按钮

- [ ] **Step 4: 验证移动端样式**

调整浏览器窗口宽度到 640px 以下：
- [ ] 确认按钮大小适配移动端
- [ ] 确认按钮间距合理
- [ ] 确认点击响应正常

- [ ] **Step 5: 提交最终版本**

```bash
git add -A
git commit -m "feat: 完成快捷按钮功能"
```

---

## 完成标准

- [ ] 通用按钮（继续、同意）始终显示
- [ ] 动态按钮根据最后一条 AI 消息智能识别
- [ ] 点击按钮直接发送消息
- [ ] 运行时隐藏动态按钮
- [ ] 移动端样式适配
- [ ] 无 TypeScript 编译错误
