# 通知开关优化实现计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 限制只有 7778 端口的后端服务才能发送通知，且启动后的首次轮询不触发通知。

**Architecture:** 在事件循环中添加首次轮询静默标志，在结果处理函数中添加端口检查逻辑，两处修改独立且简单。

**Tech Stack:** Go 1.21+

---

## 文件结构

| 文件 | 责任 | 改动类型 |
|------|------|---------|
| `cmd/kanban-watcher/main.go` | 首次轮询静默 + 端口检查 | 修改 |

**说明：** `runEventLoop` 和 `handlePollResult` 两个函数都在 `main.go` 中（第 41-197 行）。Headless 模式调用 `runEventLoop` 时自动受益于首次轮询静默逻辑。

---

## Task 1: 首次轮询静默

**Files:**
- Modify: `cmd/kanban-watcher/main.go:41-106` (runEventLoop 函数)

**改动点：**
1. 在 `var latestWorkspaces []api.EnrichedWorkspace` 之后添加 `isFirstPoll := true`
2. 在 `latestWorkspaces = result.Workspaces` 之后添加首次轮询检查逻辑

- [ ] **Step 1: 在 runEventLoop 函数中添加首次轮询静默逻辑**

在 `var latestWorkspaces []api.EnrichedWorkspace` 行后添加：

```go
// 首次轮询静默标志：启动后的第一次轮询不触发通知
isFirstPoll := true
```

在 `latestWorkspaces = result.Workspaces` 行后，`handlePollResult` 调用前添加：

```go
// 首次轮询静默：只更新菜单栏，不发送通知
if isFirstPoll {
    isFirstPoll = false
    if trayApp != nil {
        trayApp.UpdateWorkspaces(result.Workspaces)
    }
    continue
}
```

完整修改后的 `runEventLoop` 函数核心部分：

```go
// 缓存最新的工作区列表，供清理器使用
var latestWorkspaces []api.EnrichedWorkspace

// 首次轮询静默标志：启动后的第一次轮询不触发通知
isFirstPoll := true

for {
    select {
    case <-ctx.Done():
        return
    case result := <-results:
        if result.Err != nil {
            fmt.Fprintf(os.Stderr, "轮询错误: %v\n", result.Err)
            continue
        }
        if summaryFetcher != nil {
            if summaries, err := summaryFetcher.GetActiveWorkspaceSummaries(ctx); err == nil {
                result.Workspaces = mergeLocalMenuSummaries(result.Workspaces, summaries)
            }
        }
        latestWorkspaces = result.Workspaces

        // 首次轮询静默：只更新菜单栏，不发送通知
        if isFirstPoll {
            isFirstPoll = false
            if trayApp != nil {
                trayApp.UpdateWorkspaces(result.Workspaces)
            }
            continue
        }

        handlePollResult(ctx, result, sessionExtractor, cfg, wechatNotifier, tracker, trayApp, dialogNotifier)
    // ... 其余代码不变
    }
}
```

- [ ] **Step 2: 构建验证**

Run: `go build ./cmd/kanban-watcher`
Expected: 编译成功，无错误

- [ ] **Step 3: Commit**

```bash
git add cmd/kanban-watcher/main.go
git commit -m "feat: 首次轮询静默，启动时不触发通知"
```

---

## Task 2: 端口限制 - 只有 7778 端口可通知

**Files:**
- Modify: `cmd/kanban-watcher/main.go:141-197` (handlePollResult 函数)

**改动点：**
1. 在菜单栏更新后、通知逻辑前添加端口检查

- [ ] **Step 1: 在 handlePollResult 函数中添加端口检查逻辑**

在 `trayApp.UpdateWorkspaces(workspaces)` 行后添加：

```go
// 端口检查：只有主端口 7778 才能发送通知
if cfg.HTTPAPI.Port != 7778 {
    return
}
```

完整修改后的 `handlePollResult` 函数：

```go
// handlePollResult 处理单次轮询结果
// 依次执行：更新菜单栏 → 评估通知阈值 → 发送微信通知 → 持久化状态
func handlePollResult(
    ctx context.Context,
    result poller.PollResult,
    sessionExtractor *sessionlog.Extractor,
    cfg *config.Config,
    wechatNotifier *wechat.Notifier,
    tracker *wechat.Tracker,
    trayApp *tray.App,
    dialogNotifier *notify.DialogNotifier,
) {
    workspaces := result.Workspaces

    // 1. 更新菜单栏显示（所有实例都更新）
    if trayApp != nil {
        trayApp.UpdateWorkspaces(workspaces)
    }

    // 端口检查：只有主端口 7778 才能发送通知
    if cfg.HTTPAPI.Port != 7778 {
        return
    }

    // 2. 评估通知阈值，获取需要告警的工作区列表
    toNotify := tracker.ProcessWorkspaces(workspaces, result.FetchedAt)

    // 3. 发送企业微信通知
    for _, tw := range toNotify {
        if err := wechatNotifier.Send(ctx, tw); err != nil {
            fmt.Fprintf(os.Stderr, "微信发送错误 [%s]: %v\n", tw.Workspace.DisplayName, err)
        }
    }

    // 4. 本地弹框通知（如果启用）
    if cfg.Notify.Enabled && dialogNotifier != nil {
        for _, tw := range toNotify {
            title := "Kanban Watcher - 需要关注"
            message := fmt.Sprintf("工作区：%s\n状态：%s\n等待时间：%d 分钟",
                tw.Workspace.DisplayName,
                tw.Workspace.StatusText(),
                tw.ElapsedMinutes,
            )
            url := fmt.Sprintf("%s/workspace/%s", cfg.KanbanAPIURL, tw.Workspace.ID)

            clicked, err := dialogNotifier.ShowAndOpen(title, message, url)
            if err != nil {
                fmt.Fprintf(os.Stderr, "弹框失败 [%s]: %v\n", tw.Workspace.DisplayName, err)
            }
            _ = clicked
        }
    }

    // 5. 若发送了通知，更新持久化状态
    if len(toNotify) > 0 {
        if err := state.SaveState(tracker.GetState()); err != nil {
            fmt.Fprintf(os.Stderr, "警告: 保存状态: %v\n", err)
        }
    }
}
```

- [ ] **Step 2: 构建验证**

Run: `go build ./cmd/kanban-watcher`
Expected: 编译成功，无错误

- [ ] **Step 3: Commit**

```bash
git add cmd/kanban-watcher/main.go
git commit -m "feat: 端口限制，只有 7778 端口可发送通知"
```

---

## Task 3: 手动测试验证

- [ ] **Step 1: 测试首次轮询静默**

1. 启动服务（7778 端口）
2. 观察第一次轮询（15 秒内）是否不触发通知
3. 等待第二次轮询，验证正常通知（如果有需要关注的工作区）

- [ ] **Step 2: 测试端口限制**

1. 确认 7778 端口实例可以正常发送通知
2. 使用 `KANBAN_PORT=7779 kanban-watcher --headless` 启动非 7778 端口实例
3. 验证非 7778 端口实例不发送通知

- [ ] **Step 3: 组合测试**

1. 同时运行 7778 和 7779 两个实例
2. 验证只有 7778 发送通知

---

## 改动总结

| 改动点 | 文件 | 行数 |
|-------|------|------|
| 首次轮询静默 | `main.go` runEventLoop | +8 行 |
| 端口限制 | `main.go` handlePollResult | +5 行 |

**总计：约 13 行代码改动**
