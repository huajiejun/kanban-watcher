# 本地弹框通知实现计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现分级超时弹框通知，待审批 5 分钟、未读消息 10 分钟后弹出 macOS 原生对话框，用户可点击"查看"打开浏览器或"忽略"关闭。

**Architecture:**
- tracker 负责阈值判断和状态管理，复用现有 `AttentionEntry` 结构新增 `LastAlertedAt` 字段支持叠加提醒
- 新增 `notify/dialog.go` 作为 osascript 封装，负责弹框和打开浏览器
- tracker 在达到阈值时调用 dialog notifier 执行弹框

**Tech Stack:** Go, osascript, getlantern/systray

---

## Chunk 1: 状态结构扩展

### Task 1: state.go 新增 LastAlertedAt 字段

**Files:**
- Modify: `internal/state/state.go:23-28`

- [ ] **Step 1: 修改 AttentionEntry 结构**

```go
// AttentionEntry 记录某个 NotificationKey 的跟踪状态
// 用于实现"持续需要关注超过阈值才通知"的逻辑
type AttentionEntry struct {
	Key           NotificationKey // 去重键
	FirstSeenAt   time.Time       // 首次发现需要关注的时间（开始计时）
	ConfirmedAt   *time.Time      // 确认需要关注的时间（连续第二个周期）
	NotifiedAt    *time.Time      // 实际发送通知的时间（nil 表示尚未通知）
	LastAlertedAt *time.Time      // 上次弹框时间（用于叠加提醒，nil 表示从未弹框）
}
```

- [ ] **Step 2: 验证构建**

Run: `cd /Users/huajiejun/github/vibe-kanban/.vibe-kanban-workspaces/74f5-kanaban-watch-go/kanban-watcher && go build ./...`

- [ ] **Step 3: 提交**

```bash
git add internal/state/state.go
git commit -m "feat: state: 新增 LastAlertedAt 字段支持叠加提醒"
```

---

## Chunk 2: 配置结构扩展

### Task 2: config.go 新增 NotifyConfig 结构

**Files:**
- Modify: `internal/config/config.go:12-20` (Config 结构)
- Modify: `internal/config/config.go:140-163` (defaultConfig)
- Modify: `internal/config/config.go:165-206` (applyDefaults)

- [ ] **Step 1: 在 Config 结构中新增 NotifyConfig 字段**

```go
// Config kanban-watcher 的根配置结构
type Config struct {
	KanbanAPIURL     string                 `yaml:"kanban_api_url"`        // vibe-kanban API 地址
	ConversationSync ConversationSyncConfig `yaml:"conversation_sync"`     // 会话日志同步配置
	Notify           NotifyConfig           `yaml:"notify"`                // 弹框通知配置
	WeChat           WeChatConfig           `yaml:"wechat"`                // 企业微信通知配置
	WorkingHours     WorkingHours           `yaml:"working_hours"`         // 工作时间窗口
	PollIntervalSecs int                    `yaml:"poll_interval_seconds"` // 轮询间隔（秒）
	Database         DatabaseConfig         `yaml:"database"`              // 数据库配置
	HTTPAPI          HTTPAPIConfig          `yaml:"http_api"`              // 本地 HTTP API 配置
}
```

- [ ] **Step 2: 新增 NotifyConfig 结构**

在 `WeChatConfig` 之后添加：

```go
// NotifyConfig 弹框通知配置
type NotifyConfig struct {
	// 分级超时阈值（分钟）
	ApprovalThreshold int `yaml:"approval_threshold"` // 待审批超时：默认 5
	MessageThreshold int `yaml:"message_threshold"`   // 未读消息超时：默认 10

	// 叠加提醒间隔（分钟）
	// 弹框确认后，每隔这个时间再次弹框（如果问题还在）
	RepeatInterval int `yaml:"repeat_interval_minutes"` // 默认 5

	// 是否启用弹框通知
	Enabled bool `yaml:"enabled"` // 默认 true
}
```

- [ ] **Step 3: 在 defaultConfig 中设置默认值**

在 `WeChat` 配置之后添加：

```go
Notify: NotifyConfig{
	ApprovalThreshold: 5,  // 待审批 5 分钟
	MessageThreshold: 10, // 未读消息 10 分钟
	RepeatInterval:   5,  // 叠加提醒间隔 5 分钟
	Enabled:          true,
},
```

- [ ] **Step 4: 在 applyDefaults 中添加默认值填充**

```go
// 在 applyDefaults 函数末尾添加：
if cfg.Notify.ApprovalThreshold <= 0 {
	cfg.Notify.ApprovalThreshold = 5
}
if cfg.Notify.MessageThreshold <= 0 {
	cfg.Notify.MessageThreshold = 10
}
if cfg.Notify.RepeatInterval <= 0 {
	cfg.Notify.RepeatInterval = 5
}
```

- [ ] **Step 5: 验证构建**

Run: `go build ./...`

- [ ] **Step 6: 提交**

```bash
git add internal/config/config.go
git commit -m "feat: config: 新增 NotifyConfig 结构支持分级阈值"
```

---

## Chunk 3: osascript 弹框封装

### Task 3: notify/dialog.go 实现弹框函数

**Files:**
- Create: `internal/notify/dialog.go`
- Create: `internal/notify/dialog_test.go`

- [ ] **Step 1: 创建 dialog.go 文件**

```go
package notify

import (
	"fmt"
	"os/exec"
	"strings"
)

// DialogNotifier 负责显示 macOS 原生弹框
type DialogNotifier struct{}

// NewDialogNotifier 创建弹框通知器
func NewDialogNotifier() *DialogNotifier {
	return &DialogNotifier{}
}

// ShowAndOpen 显示弹框，根据用户选择决定是否打开浏览器
// 返回：用户是否点击了"查看"
func (d *DialogNotifier) ShowAndOpen(title, message, url string) (bool, error) {
	script := fmt.Sprintf(
		`display dialog "%s" with title "%s" buttons {"查看", "忽略"} default button "查看"`,
		escapeAppleScript(message),
		escapeAppleScript(title),
	)

	cmd := exec.Command("osascript", "-e", script)
	output, err := cmd.CombinedOutput()
	result := strings.TrimSpace(string(output))

	// 解析结果判断用户点击的按钮
	if strings.Contains(result, "查看") {
		// 用户点击"查看"，打开浏览器
		if err := exec.Command("open", url).Run(); err != nil {
			return true, fmt.Errorf("打开浏览器失败: %w", err)
		}
		return true, nil
	}

	if strings.Contains(result, "忽略") {
		return false, nil
	}

	// 其他情况（错误、超时等），视为用户取消
	return false, fmt.Errorf("unexpected dialog result: %s", result)
}

// escapeAppleScript 转义 AppleScript 特殊字符
func escapeAppleScript(s string) string {
	s = strings.ReplaceAll(s, `"`, `\"`)
	s = strings.ReplaceAll(s, `\`, `\\`)
	return s
}
```

- [ ] **Step 2: 创建 dialog_test.go 文件**

```go
package notify

import (
	"testing"
)

func TestEscapeAppleScript(t *testing.T) {
	tests := []struct {
		input    string
		expected string
	}{
		{`hello`, `hello`},
		{`hello"world`, `hello\"world`},
		{`hello\world`, `hello\\world`},
		{`hello"\"world`, `hello\\"\\\"world`},
	}

	for _, tt := range tests {
		result := escapeAppleScript(tt.input)
		if result != tt.expected {
			t.Errorf("escapeAppleScript(%q) = %q, want %q", tt.input, result, tt.expected)
		}
	}
}
```

- [ ] **Step 3: 运行测试验证**

Run: `go test ./internal/notify/... -v`

- [ ] **Step 4: 提交**

```bash
git add internal/notify/dialog.go internal/notify/dialog_test.go
git commit -m "feat: notify: 添加 osascript 弹框封装"
```

---

## Chunk 4: tracker 逻辑扩展

### Task 4: tracker.go 支持分级阈值和叠加提醒

**Files:**
- Modify: `internal/wechat/tracker.go:17-22` (Tracker 结构)
- Modify: `internal/wechat/tracker.go:26-50` (NewTracker 函数)
- Modify: `internal/wechat/tracker.go:63-159` (ProcessWorkspaces 函数)

- [ ] **Step 1: 修改 Tracker 结构支持分级阈值**

```go
// Tracker 管理通知去重和阈值判断逻辑
// 非并发安全，应由单 goroutine 调用（通过 channel 串行化）
type Tracker struct {
	current           state.AppState // 当前跟踪状态
	approvalThreshold int           // 待审批超时（分钟）
	messageThreshold int           // 未读消息超时（分钟）
	repeatInterval   int           // 叠加提醒间隔（分钟）
}
```

- [ ] **Step 2: 修改 NewTracker 函数签名**

```go
// NewTracker 使用持久化状态创建跟踪器
// 启动时从磁盘加载上次保存的状态，确保跨进程去重
func NewTracker(s state.AppState, approvalThreshold, messageThreshold, repeatInterval int) *Tracker {
	// ... 保留现有兼容性迁移逻辑 ...
	return &Tracker{
		current:           s,
		approvalThreshold: approvalThreshold,
		messageThreshold: messageThreshold,
		repeatInterval:   repeatInterval,
	}
}
```

- [ ] **Step 3: 新增 getThreshold 方法**

在 `notificationKey` 函数之后添加：

```go
// getThreshold 根据工作区类型返回超时阈值
func (t *Tracker) getThreshold(w api.EnrichedWorkspace) int {
	if w.Summary.HasPendingApproval {
		return t.approvalThreshold
	}
	if w.Summary.HasUnseenTurns {
		return t.messageThreshold
	}
	return 0 // 不需要关注
}
```

- [ ] **Step 4: 新增 shouldShowDialog 方法**

```go
// shouldShowDialog 检查是否应该弹框（基于 LastAlertedAt 和 repeatInterval）
func (t *Tracker) shouldShowDialog(entry state.AttentionEntry, now time.Time) bool {
	if entry.LastAlertedAt == nil {
		return true
	}
	elapsed := now.Sub(*entry.LastAlertedAt)
	return elapsed >= time.Duration(t.repeatInterval)*time.Minute
}
```

- [ ] **Step 5: 修改 ProcessWorkspaces 中的阈值判断逻辑**

将现有的单一 threshold 判断替换为分级判断：

```go
// 在 ProcessWorkspaces 函数中，找到：
threshold := time.Duration(t.thresholdMinutes) * time.Minute
// 替换为在循环内根据工作区类型动态获取阈值：
threshold := time.Duration(t.getThreshold(w)) * time.Minute
if threshold == 0 {
	continue // 不需要关注
}
```

- [ ] **Step 6: 在达到阈值后添加叠加提醒判断**

找到"达到阈值：标记为已通知"的逻辑，在 `toNotify = append` 之前添加：

```go
// 检查是否应该弹框（叠加提醒）
if !t.shouldShowDialog(existing, now) {
	continue
}
```

- [ ] **Step 7: 验证构建**

Run: `go build ./...`

- [ ] **Step 8: 提交**

```bash
git add internal/wechat/tracker.go
git commit -m "feat: tracker: 支持分级阈值和叠加提醒"
```

---

## Chunk 5: 集成到事件循环

### Task 5: run.go 集成 dialog notifier

**Files:**
- Modify: `cmd/kanban-watcher/run.go` (runDaemon 函数)

- [ ] **Step 1: 添加 notify 包导入**

在 `run.go` 的导入部分添加：

```go
"github.com/huajiejun/kanban-watcher/internal/notify"
```

- [ ] **Step 2: 修改 runDaemon 中 tracker 初始化**

找到：
```go
tracker := wechat.NewTracker(persistedState, cfg.WeChat.NotifyThresholdMinutes)
```

替换为：
```go
tracker := wechat.NewTracker(
	persistedState,
	cfg.Notify.ApprovalThreshold,
	cfg.Notify.MessageThreshold,
	cfg.Notify.RepeatInterval,
)
```

- [ ] **Step 3: 在 wechatNotifier 初始化后添加 dialogNotifier**

找到：
```go
wechatNotifier := wechat.NewNotifier(cfg.WeChat)
```

在之后添加：
```go
dialogNotifier := notify.NewDialogNotifier()
```

- [ ] **Step 4: 修改事件循环中的通知调用**

找到 `runEventLoop` 函数中的通知调用逻辑，在 `wechatNotifier.Send` 之后添加弹框逻辑：

```go
for _, tw := range toNotify {
	// 1. 企业微信通知（已有）
	if err := wechatNotifier.Send(ctx, tw); err != nil {
		log.Printf("企业微信通知失败: %v", err)
	}

	// 2. 本地弹框通知（新增）
	if cfg.Notify.Enabled {
		if t.shouldShowDialog(...) { // 需要获取对应的 entry
			title := "Kanban Watcher - 需要关注"
			message := fmt.Sprintf("工作区：%s\n状态：%s\n等待时间：%d 分钟",
				tw.Workspace.DisplayName,
				tw.Workspace.StatusText(),
				tw.ElapsedMinutes,
			)
			url := fmt.Sprintf("%s/workspace/%s", cfg.KanbanAPIURL, tw.Workspace.ID)

			clicked, err := dialogNotifier.ShowAndOpen(title, message, url)
			if err != nil {
				log.Printf("弹框失败: %v", err)
			}

			// 更新 LastAlertedAt
			if clicked || err == nil {
				key := notificationKey(tw.Workspace)
				if entry, ok := tracker.GetState().Entries[key]; ok {
					now := time.Now()
					entry.LastAlertedAt = &now
					tracker.GetState() = tracker.GetState().WithEntry(key, entry)
				}
			}
		}
	}
}
```

**注意**：实际实现时需要更仔细地处理状态更新逻辑，确保在正确的位置更新 `LastAlertedAt`。

- [ ] **Step 5: 验证构建**

Run: `go build ./...`

- [ ] **Step 6: 提交**

```bash
git add cmd/kanban-watcher/run.go
git commit -m "feat: run: 集成 dialog notifier 到事件循环"
```

---

## Chunk 6: 配置示例更新

### Task 6: 更新 config.yaml.example

**Files:**
- Modify: `config.yaml.example`

- [ ] **Step 1: 添加 notify 配置节**

```yaml
notify:
  # 超时阈值配置（分钟）
  approval_threshold: 5    # 待审批超时
  message_threshold: 10   # 未读消息超时
  # 叠加提醒间隔（分钟）
  repeat_interval_minutes: 5
  # 是否启用弹框通知
  enabled: true
```

- [ ] **Step 2: 提交**

```bash
git add config.yaml.example
git commit -m "docs: config: 添加 notify 配置示例"
```
