# 弹出卡片优化-测试概述

日期：2026-03-23

## 自动化验证

### 1. 卡片与参数测试

命令：

```bash
npm test -- --run tests/kanban-watcher-card.test.ts tests/playground.test.ts
```

结果：

- 2 个测试文件通过
- 52 个测试通过
- 0 个测试失败

覆盖重点：

- API 消息弹窗正常加载
- `tool_use` 消息渲染为工具摘要卡片
- 工具摘要卡片显示工具名、摘要和状态类名
- 原有普通消息渲染无回归
- playground 参数解析无回归

### 2. 构建验证

命令：

```bash
npm run build
```

结果：

- 构建成功
- 产物输出正常

## 已验证场景

- 工具调用摘要卡片插入到现有消息流中
- `tool_use` 消息默认走弱化单行摘要结构
- `command_run` 类型工具调用可以展示命令摘要
- `running` 状态会落到工具卡片状态类名
- 原有 API 弹窗消息加载、发送消息、实时追加测试保持通过

## 待手动验证

根据仓库约定，本次未主动执行网页端手工测试，待用户手测以下场景：

- 手机端弹窗中工具摘要卡片的一行展示密度
- 点击工具卡片后的展开详情样式
- 多个连续工具调用时的阅读节奏
- `pending_approval`、`error` 等状态在真实数据下的视觉表现
