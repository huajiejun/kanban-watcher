# 快捷网页入口-测试概述(中文)

## 测试时间

- 2026-03-26 23:39:46 CST

## 测试环境

- 项目：`kanban-watcher`
- 前端框架：Lit + Vite
- 测试工具：Vitest
- 验证范围：工作区快捷网页入口的桌面端弹层、手机端全屏、无地址隐藏、关闭链路

## 本次改动覆盖点

- 工作区表头新增快捷网页入口按钮，位置在启动/暂停按钮左侧
- 有 `browser_url` 时显示按钮，无地址时隐藏
- 宽屏端点击后打开居中大面板 iframe 弹层
- 手机端点击后使用全屏 iframe 弹层
- 点击关闭按钮、点击遮罩、关闭工作区窗格时都会关闭快捷网页弹层

## 自动化验证

### 1. 目标测试红绿验证

执行命令：

```bash
npm test -- tests/workspace-home.test.ts -t "renders the workspace web preview button before the dev-server toggle when browser_url is available|opens a desktop web preview modal with iframe when the workspace web preview button is clicked"
```

结果：

- 首轮为红灯，失败点为：
  - 未渲染 `.dialog-web-preview`
  - 点击 `.dialog-web-preview` 时目标为空
- 补最小实现后转绿

### 2. 第二轮行为测试

执行命令：

```bash
npm test -- tests/workspace-home.test.ts -t "uses a full-screen web preview layout on mobile|does not render the workspace web preview button when browser_url is missing|closes the web preview overlay when the close button is clicked|closes the web preview overlay when the backdrop is clicked|closes the web preview overlay when the workspace pane is closed"
```

结果：

- 首轮为红灯，失败点为：
  - 手机端布局仍按 `desktop` 路径渲染
  - 关闭工作区窗格后快捷网页弹层仍存在
- 修复后 5 条测试全部通过

### 3. 模块级回归

执行命令：

```bash
npm test -- tests/workspace-home.test.ts
```

结果：

- `47/47` 通过

### 4. 构建验证

执行命令：

```bash
npm run build:web
```

结果：

- 构建成功
- 关键输出：
  - `dist/web/index.html`
  - `dist/web/preview/index.html`

## 全量测试现状

执行命令：

```bash
npm test
```

结果：

- 失败，存在仓库内既有失败项，与本次快捷网页入口改动不在同一范围
- 主要失败文件：
  - `tests/workspace-pane-layout.test.ts`
  - `tests/playground.test.ts`
  - `tests/todo-integration.test.ts`
  - `tests/todo-progress-popup.test.ts`
  - `tests/kanban-watcher-card.test.ts`

说明：

- 本次功能相关的 `tests/workspace-home.test.ts` 已全量通过
- 当前不建议把全仓失败项混入本需求一起处理，否则会扩大改动面

## 人工截图验证

- 当前未生成截图

原因：

- 仓库内缺少 `webapp-testing` 技能建议使用的 `scripts/with_server.py`
- 当前工作区也未安装 Playwright 依赖，无法在不额外引入工具链的前提下完成自动截图

建议：

- 若后续需要补齐截图证据，可在补上浏览器自动化依赖后生成以下文件：
  - `test-images/快捷网页入口需求/桌面端-快捷网页弹层.png`
  - `test-images/快捷网页入口需求/手机端-快捷网页弹层.png`

## 结论

- 本次“快捷网页入口”相关实现已通过目标测试、模块级回归和前端构建验证
- 全量测试仍存在仓库既有失败项，暂不归因于本次改动
