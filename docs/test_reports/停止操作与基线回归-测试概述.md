# 停止操作与基线回归-测试概述

## 测试日期

- 2026-03-24

## 测试范围

- 工作区停止当前执行链路
- 工作区消息时间显示与卡片基线行为
- playground 默认 API 预览参数
- `scripts` 目录脚本入口整理后的全量测试基线

## 执行命令

### Go 全量测试

```bash
go test ./...
```

结果：

- 通过
- `scripts/check_kw_msg_contexts`
- `scripts/run_kw_sync_once`
  已拆分为独立子目录入口，不再影响全量测试

### 前端全量测试

```bash
npm test
```

结果：

- 5 个测试文件通过
- 74 个测试通过
- 0 个测试失败

### 前端构建验证

```bash
npm run build
```

结果：

- 构建成功

## 关键验证结论

- 停止接口链路保持可用，之前已覆盖本地 API 停止与队列取消的自动化测试
- 卡片时间显示重新兼容 `completed_at` 与实体级 `updated_at` 兜底，预览场景不再统一显示为 `recently`
- playground 测试基线已与当前默认直连配置对齐
- `go test ./...` 基线已恢复，可作为后续回归入口

## 人工联调记录

- 用户手测反馈：重启后点击取消，`vibe-kanban` 侧已成功取消当前执行

## 截图

- 本轮未新增截图
