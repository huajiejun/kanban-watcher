# 启动脚本角色提示-测试概述

## 测试目标

- 验证 `scripts/start-dev.sh start` 输出会显示当前运行角色
- 验证 `scripts/start-dev.sh start` 输出会显示 WebSocket 主后端地址
- 验证新增提示不影响既有前端端口分配与后端端口推导逻辑

## 已执行命令

1. `bash ./scripts/test_start_dev_frontend_port_api.sh`
   - 结果：PASS

## 覆盖点

- 启动脚本仍会调用固定管理 API `http://127.0.0.1:7778/api/workspace/{workspace_id}/frontend-port`
- 启动脚本仍按 `frontend_port + 10000` 推导后端端口
- 启动输出新增：
  - `运行角色: worker`
  - `WebSocket 主后端: http://127.0.0.1:7778`

## 结论

- 对通过 `start-dev.sh` 启动的业务后端，脚本会直接提示当前是 `worker` 角色
- 脚本会明确提示页面的实时连接将统一走 `7778` 主后端
- 现有端口分配与前后端启动链路未被破坏
