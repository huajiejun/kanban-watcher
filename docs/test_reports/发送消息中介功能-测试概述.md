# 发送消息中介功能-测试概述

## 测试日期

- 2026-03-24

## 测试范围

- 后端消息上下文存储
- 同步阶段从 execution process 提取 `executor_config`
- 统一消息发送接口 `/api/workspace/{id}/message`
- 前端卡片真实接入 `send/queue` 模式

## 执行命令

### Go 全量测试

```bash
go test ./...
```

结果：

- 通过

### 前端全量测试

```bash
npm test
```

结果：

- 通过

### 前端关键卡片测试

```bash
npm test -- tests/kanban-watcher-card.test.ts
```

结果：

- 通过

## 关键验证结论

- 新增 `kw_msg_contexts` 后，工作区消息上下文可在本地库持久化
- execution process 流中的 `executor_action.typ.executor_config` 已被解析并写入消息上下文
- 卡片发送消息已从旧 `follow-up` 单接口切换到统一 `/message` 接口
- 运行中工作区的“加入队列”操作已走真实后端接口，不再是前端本地伪队列

## 未覆盖项

- 本轮未做人工联调截图
- 本轮未验证真实 `vibe-kanban` 运行中 queue 状态在 watcher UI 中的额外展示细节
- 本轮未加入“上下文缺失时远端补拉”的实现

## 截图

- 暂无
