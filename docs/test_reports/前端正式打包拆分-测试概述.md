# 前端正式打包拆分-测试概述

## 测试目标

验证 Home Assistant 卡片构建与网页端正式构建已经拆分为独立命令、独立配置文件和独立产物目录。

## 测试记录

### 1. 定向配置测试

命令：

```bash
npm test -- tests/build-config.test.ts
```

结果：

- 通过
- 验证了 `build:ha`、`build:web`、聚合 `build` 三个脚本
- 验证了 `vite.config.ha.ts` 输出到 `dist/ha`
- 验证了 `vite.config.web.ts` 输出到 `dist/web`

### 2. Home Assistant 构建

命令：

```bash
npm run build:ha
```

结果：

- 通过
- 产物：`dist/ha/kanban-watcher-card.js`

### 3. 网页端构建

命令：

```bash
npm run build:web
```

结果：

- 通过
- 产物：
  - `dist/web/index.html`
  - `dist/web/preview/index.html`
  - `dist/web/assets/*`

### 4. 聚合构建

命令：

```bash
npm run build
```

结果：

- 通过
- 依次执行 `build:ha` 与 `build:web`

### 5. 全量单元测试

命令：

```bash
npm test
```

结果：

- 未通过
- 当前输出为 `18 failed | 175 passed`
- 失败主要集中在现有 UI/交互测试：
  - `tests/kanban-watcher-card.test.ts`
  - `tests/playground.test.ts`
  - `tests/todo-integration.test.ts`
  - `tests/todo-progress-popup.test.ts`

说明：

- 本次改动新增的 `tests/build-config.test.ts` 已通过
- 两条正式构建链路与聚合构建都已成功执行
- 仓库当前仍存在与本次打包拆分无直接关联的既有失败测试，需要单独排查
