# 预制按钮规则调整-测试概述

## 测试目标

验证以下变更：

1. 非方案类 LLM prompt 改为输出 AI AGENT 后续可以做且容易做的动作
2. 禁止动作支持配置透传与过滤
3. 编号列表、项目符号列表、自然语言动作拆分的正则兜底更准确

## 执行记录

### 1. 定向测试

执行命令：

```bash
npm test -- tests/playground.test.ts tests/quick-buttons.test.ts tests/quick-buttons-llm.test.ts
```

结果：

- 3 个测试文件全部通过
- 44 个测试全部通过

覆盖点：

- 预览配置支持 `quick_button_forbidden_actions` 参数解析与卡片配置透传
- decision prompt 包含 `AI AGENT`、`后续可以做什么` 和禁止动作约束
- 默认黑名单会过滤 `提交代码`、`合并代码`、`部署` 等动作
- `1. 方案A`、`- 补充测试` 等文本会提取出真实动作文本

### 2. 全量回归

执行命令：

```bash
npm test
```

结果：

- 本次改动相关测试保持通过
- 仓库仍存在一批与本需求无直接关联的既有失败测试

## 结论

本次“预制按钮规则调整”目标功能已通过定向测试验证。全量回归存在历史基线失败，已单独记录 issue 以便后续排查。
