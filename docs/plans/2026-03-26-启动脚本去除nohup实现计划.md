# 启动脚本去除nohup Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `scripts/start-dev.sh` 在不使用 `nohup` 启动后端的前提下，仍保持后台启动、正确记录后端 PID，并继续把 stderr 镜像到控制台。

**Architecture:** 继续复用现有 stderr 镜像方案，但把后端启动改为普通后台 `go run ... &`；先扩展脚本级测试锁住“PID 文件必须指向真实后端进程”这一行为，再做最小实现。

**Tech Stack:** Bash, shell test, `tee`, `mkfifo`

---

## Chunk 1: 测试先行

### Task 1: 收紧脚本级回归测试

**Files:**
- Modify: `scripts/test_start_dev_backend_console_logging.sh`
- Modify: `scripts/start-dev.sh`

- [ ] **Step 1: 写失败断言**

让假 `go` 进程把自己的 PID 写到临时文件，脚本启动完成后断言 `/tmp/kanban-dev/backend-<port>.pid` 与该 PID 一致。

- [ ] **Step 2: 运行测试确认失败**

Run: `bash scripts/test_start_dev_backend_console_logging.sh`
Expected: FAIL，提示脚本记录的后端 PID 不是实际后端进程

## Chunk 2: 最小实现

### Task 2: 去掉后端 nohup 并恢复正确后台行为

**Files:**
- Modify: `scripts/start-dev.sh`

- [ ] **Step 1: 后端改为普通后台执行**

将后端启动改为：

```bash
go run ./cmd/kanban-watcher >> "$BACKEND_LOG_FILE" 2> "$BACKEND_STDERR_PIPE" &
```

- [ ] **Step 2: 保留现有 stderr 镜像与 PID 检查**

确保 `$!` 指向真实 `go run` 进程，启动检测和 stop 清理逻辑仍然有效。

## Chunk 3: 验证

### Task 3: 验证后台行为

**Files:**
- Verify: `scripts/test_start_dev_backend_console_logging.sh`

- [ ] **Step 1: 运行脚本测试**

Run: `bash scripts/test_start_dev_backend_console_logging.sh`
Expected: PASS
