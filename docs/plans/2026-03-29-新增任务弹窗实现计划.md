# 新增任务弹窗实现计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复并完善移动端看板的新建任务弹窗，确保 FAB 按钮能稳定打开弹窗，创建成功后刷新看板

**Architecture:** 通过 CustomEvent 驱动弹窗显示，dialog 组件负责表单收集和 API 调用，board 组件负责渲染控制和事件监听。错误处理在 dialog 层完成，成功后通过事件通知 board 刷新。

**Tech Stack:** Lit Web Components, TypeScript, fetch API

---

## 文件结构

| 文件 | 职责 | 变更类型 |
|------|------|----------|
| `src/components/mobile-issue-create-dialog.ts` | 创建任务弹窗组件：表单渲染、验证、API调用、错误提示 | Modify |
| `src/components/mobile-kanban-board.ts` | 看板主组件：控制 dialog 渲染、处理打开事件、监听创建成功刷新列表 | Modify |
| `src/lib/issue-api.ts` | API 层：`createIssue` 函数（已有，无需改动） | Read-only |

---

## 前置检查

### Task 0: 检查类型定义和现有事件监听

**Files:**
- Read: `src/types/issue.ts:36-42`
- Read: `src/components/mobile-issue-create-dialog.ts:198-212`

- [ ] **Step 1: 确认 CreateIssuePayload 包含 project_id**

检查 `CreateIssuePayload` 接口：

```typescript
export interface CreateIssuePayload {
  title: string;
  description?: string;
  priority?: string;
  status_id?: string;
  project_id?: string;  // 确认有此字段
}
```

如无 `project_id` 字段，先添加：

```typescript
export interface CreateIssuePayload {
  title: string;
  description?: string;
  priority?: string;
  status_id?: string;
  project_id?: string;  // 添加
}
```

- [ ] **Step 2: 确认 dialog 已监听 open 事件**

检查 `mobile-issue-create-dialog.ts` 的 `connectedCallback`：

```typescript
connectedCallback() {
  super.connectedCallback();
  this.addEventListener("open", this.handleOpen);  // 确认有此行
}
```

- [ ] **Step 3: Commit（如有修改）**

```bash
git add -A
git commit -m "fix: 确保 CreateIssuePayload 支持 project_id"
```

---

## Chunk 1: Dialog 组件增强

### Task 1: 添加 projectId 属性支持

**Files:**
- Modify: `src/components/mobile-issue-create-dialog.ts:179-196`

- [ ] **Step 1: 添加 projectId 属性和类型定义**

在 `MobileIssueCreateDialog` 类中添加：

```typescript
projectId = "";

static properties = {
  statuses: { type: Array },
  baseUrl: { type: String },
  apiKey: { type: String },
  projectId: { type: String },  // 新增
  visible: { type: Boolean },
  submitting: { type: Boolean },
};
```

- [ ] **Step 2: 修改 handleSubmit 加入 project_id**

在 `handleSubmit` 方法中，修改 `createIssue` 调用：

```typescript
await createIssue(
  { baseUrl: this.baseUrl, apiKey: this.apiKey },
  {
    title: this.title.trim(),
    description: this.description.trim() || undefined,
    priority: this.priority,
    status_id: this.selectedStatusId || undefined,
    project_id: this.projectId,  // 新增
  }
);
```

- [ ] **Step 3: Commit**

```bash
git add src/components/mobile-issue-create-dialog.ts
git commit -m "feat: 新增任务弹窗添加 projectId 属性并加入请求体"
```

---

### Task 2: 增加错误提示功能

**Files:**
- Modify: `src/components/mobile-issue-create-dialog.ts:221-246`

- [ ] **Step 1: 添加错误状态属性**

在类中添加：

```typescript
errorMessage = "";

static properties = {
  // ... 已有属性
  errorMessage: { type: String },  // 新增
};
```

- [ ] **Step 2: 修改 handleSubmit 增加错误处理**

```typescript
private async handleSubmit() {
  if (!this.title.trim() || this.submitting) return;
  this.submitting = true;
  this.errorMessage = "";  // 清空错误

  try {
    await createIssue(
      { baseUrl: this.baseUrl, apiKey: this.apiKey },
      {
        title: this.title.trim(),
        description: this.description.trim() || undefined,
        priority: this.priority,
        status_id: this.selectedStatusId || undefined,
        project_id: this.projectId,
      }
    );
    this.close();
    this.dispatchEvent(
      new CustomEvent("issue-created", {
        bubbles: true,
        composed: true,
      })
    );
  } catch (err) {
    console.error("创建任务失败:", err);
    // 错误映射
    const errorText = err instanceof Error ? err.message : String(err);
    if (errorText.includes("400") || errorText.includes("422")) {
      this.errorMessage = "创建失败，请检查必填项是否完整";
    } else if (errorText.includes("401") || errorText.includes("403")) {
      this.errorMessage = "权限不足，无法创建任务";
    } else if (errorText.includes("fetch") || errorText.includes("network")) {
      this.errorMessage = "请求失败，请检查网络连接";
    } else {
      this.errorMessage = "服务异常，请稍后重试";
    }
  } finally {
    this.submitting = false;
  }
}
```

- [ ] **Step 3: 添加错误提示 UI**

在 `render()` 方法的 dialog-footer 之前添加错误提示：

```typescript
${this.errorMessage
  ? html`<div class="error-message">${this.errorMessage}</div>`
  : nothing}
```

在样式中添加：

```css
.error-message {
  padding: 8px 16px;
  color: #f87171;
  font-size: 0.8rem;
  text-align: center;
}
```

- [ ] **Step 4: Commit**

```bash
git add src/components/mobile-issue-create-dialog.ts
git commit -m "feat: 新增任务弹窗增加错误提示功能"
```

---

### Task 3: 优化关闭/取消时重置错误

**Files:**
- Modify: `src/components/mobile-issue-create-dialog.ts:214-219`

- [ ] **Step 1: 修改 close 方法清空错误**

```typescript
private close() {
  this.visible = false;
  this.title = "";
  this.description = "";
  this.priority = "medium";
  this.errorMessage = "";  // 新增
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/mobile-issue-create-dialog.ts
git commit -m "fix: 关闭弹窗时清空错误提示"
```

---

## Chunk 2: Board 组件修复

### Task 4: 确保 Dialog 正确渲染

**Files:**
- Read: `src/components/mobile-kanban-board.ts:340-350`

- [ ] **Step 1: 确认 dialog 渲染条件**

检查 board 模板中 dialog 的渲染位置。确认它在非 loading 状态且 `selectedProjectId` 存在时渲染：

```typescript
// 应该在 render() 的某个条件分支中，如：
${this.selectedProjectId && !this.loading
  ? html`
    <mobile-issue-create-dialog
      .statuses=${this.columns.map((c) => c.status)}
      .baseUrl=${this.baseUrl}
      .apiKey=${this.apiKey}
      .projectId=${this.selectedProjectId}  <!-- 确保传入 -->
      @issue-created=${this.handleIssueCreated}
    ></mobile-issue-create-dialog>
  `
  : nothing}
```

- [ ] **Step 2: 如无 projectId 绑定则添加**

如果上述位置没有 `.projectId` 绑定，添加之。

- [ ] **Step 3: Commit**

```bash
git add src/components/mobile-kanban-board.ts
git commit -m "feat: 看板组件传入 projectId 给创建弹窗"
```

---

### Task 5: 修复打开逻辑增加存在性检查

**Files:**
- Modify: `src/components/mobile-kanban-board.ts:242-251`

- [ ] **Step 1: 增强 openCreateDialog 方法**

```typescript
private openCreateDialog() {
  const dialog = this.renderRoot.querySelector(
    "mobile-issue-create-dialog"
  );
  if (dialog) {
    dialog.dispatchEvent(
      new CustomEvent("open", { bubbles: true, composed: true })
    );
  } else {
    console.warn("[openCreateDialog] dialog 元素未找到");
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/mobile-kanban-board.ts
git commit -m "fix: 打开创建弹窗时增加元素存在性检查"
```

---

### Task 6: 实现创建成功后刷新看板

**Files:**
- Read: `src/components/mobile-kanban-board.ts:159-165`（确认 loadBoard 存在）
- Modify: `src/components/mobile-kanban-board.ts`（添加事件处理器）

- [ ] **Step 1: 添加 issue-created 事件处理器**

在 `MobileKanbanBoard` 类中添加方法（参考现有方法风格）：

```typescript
private handleIssueCreated = () => {
  // 使用现有的 loadBoard 方法刷新（silent=true 避免显示加载状态）
  if (this.selectedProjectId) {
    this.loadBoard(true);
  }
};
```

- [ ] **Step 2: 绑定事件处理器**

确保 dialog 模板中有事件绑定（约 346 行）：

```typescript
@issue-created=${this.handleIssueCreated}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/mobile-kanban-board.ts
git commit -m "feat: 创建成功后自动刷新看板"
```

---

### Task 7: 确保 FAB 按钮和 Dialog 在正确条件下渲染

**Files:**
- Modify: `src/components/mobile-kanban-board.ts:334-347`

- [ ] **Step 1: 修改 FAB 按钮条件渲染**

当前 FAB 按钮（334-341 行）无条件渲染。修改为：

```typescript
${this.selectedProjectId && !this.loading
  ? html`
    <button
      class="fab-create"
      type="button"
      @click=${this.openCreateDialog}
      title="新建任务"
    >
      +
    </button>
  `
  : nothing}
```

- [ ] **Step 2: 修改 Dialog 条件渲染**

当前 dialog（342-347 行）也是无条件渲染。修改为：

```typescript
${this.selectedProjectId && !this.loading
  ? html`
    <mobile-issue-create-dialog
      .statuses=${this.columns.map((c) => c.status)}
      .baseUrl=${this.baseUrl}
      .apiKey=${this.apiKey}
      .projectId=${this.selectedProjectId}
      @issue-created=${this.handleIssueCreated}
    ></mobile-issue-create-dialog>
  `
  : nothing}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/mobile-kanban-board.ts
git commit -m "fix: FAB 按钮和创建弹窗仅在正确条件下渲染"
```

---

## Chunk 3: 验证与测试

### Task 8: TypeScript 类型检查

**Files:**
- All modified files

- [ ] **Step 1: 运行类型检查**

```bash
npx tsc --noEmit
```

- [ ] **Step 2: 如有错误则修复**

常见错误可能包括：
- `project_id` 不在 `CreateIssuePayload` 中（需要检查类型定义）
- 方法名不匹配

- [ ] **Step 3: Commit（如有修复）**

```bash
git add -A
git commit -m "fix: 修复 TypeScript 类型错误"
```

---

### Task 9: 构建验证

**Files:**
- All modified files

- [ ] **Step 1: 运行 Vite 构建**

```bash
npx vite build
```

- [ ] **Step 2: 确保构建成功**

- [ ] **Step 3: Commit（如有修复）**

```bash
git add -A
git commit -m "chore: 构建验证通过"
```

---

### Task 10: 功能验证

**Files:**
- All modified files
- Test: 手动测试

- [ ] **Step 1: 验证 FAB 按钮显示/隐藏**

启动开发服务器：

```bash
npm run dev
```

测试场景：
1. 无项目选择时：底部不应显示 `+` 按钮
2. 选择项目后：应显示 `+` 按钮
3. 加载中时：按钮应隐藏

- [ ] **Step 2: 验证弹窗打开**

1. 点击 `+` 按钮
2. 预期：底部滑出创建弹窗
3. 预期：状态默认选中第一列，优先级默认 medium

- [ ] **Step 3: 验证表单校验**

1. 标题为空时，创建按钮应禁用
2. 输入标题后，创建按钮应启用

- [ ] **Step 4: 验证创建成功流程**

1. 填写有效信息（标题必填，其他可选）
2. 点击创建
3. 预期：弹窗关闭，看板自动刷新，新任务出现在对应列

- [ ] **Step 5: 验证错误处理**

1. 模拟网络错误（如断网）
2. 点击创建
3. 预期：弹窗不关闭，显示"请求失败，请检查网络连接"

- [ ] **Step 6: 验证取消/关闭**

1. 填写部分内容
2. 点击取消或遮罩关闭
3. 预期：弹窗关闭，再次打开时表单已重置

- [ ] **Step 7: Commit（记录测试结果）**

```bash
git add -A
git commit -m "test: 新增任务弹窗功能验证通过"
```

---

## 计划审查

使用 plan-document-reviewer 子代理审查本计划。
