# 手机端预制按钮收缩 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在手机端默认只展示一行预制按钮，其余按钮收起，用户点击后再展开查看全部。

**Architecture:** 在 `workspace-conversation-pane` 组件层统一包裹快捷按钮区域，基于移动端宽度和按钮实际高度判断是否需要收缩。桌面端保持现状不变。

**Tech Stack:** TypeScript, Lit, Vitest

---

## Chunk 1: 测试与实现

### Task 1: 为手机端快捷按钮收缩新增测试并实现

**Files:**
- Modify: `src/components/workspace-conversation-pane.ts`
- Modify: `src/styles.ts`
- Modify: `tests/workspace-conversation-pane.test.ts`

- [ ] **Step 1: 写失败测试**
- [ ] **Step 2: 运行测试确认失败**
- [ ] **Step 3: 写最小实现**
- [ ] **Step 4: 运行测试确认通过**
