# 对话消息 Redis 优先查询实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 工作区对话消息从 Redis 直接读取（flush 前即可见），默认 50 条 cursor 分页，跨 processID 汇总

**Architecture:** 后端新增 Redis 查询层（redis_reader.go）+ workspace 级 process 索引 ZSET + 新 API 端点。前端改为调用新 API，支持上划加载更多。

**Tech Stack:** Go (go-redis/v8), TypeScript (Lit), Redis ZSET/Hash

**Spec:** `docs/plans/2026-03-29-对话消息Redis优先查询设计.md`

---

## File Structure

| Action | File | Responsibility |
|--------|------|---------------|
| Modify | `internal/buffer/redis_buffer.go` | Enqueue 追加 workspace 索引 ZADD |
| Create | `internal/buffer/redis_reader.go` | Redis 优先查询逻辑 + cursor 解析/编码 |
| Create | `internal/buffer/redis_reader_test.go` | redis_reader 单元测试 |
| Modify | `internal/api/messages.go` | 新增 `handleWorkspaceMessages` handler + 响应类型 |
| Modify | `internal/server/server.go` | `handleWorkspaces` 中注册 `/messages` 路由 + 传递 rdb |
| Modify | `src/lib/http-api.ts` | 新增 `fetchWorkspaceMessages` 函数 |
| Modify | `src/web/workspace-home.ts` | `loadWorkspaceMessages` 改用新 API + `loadMoreMessages` |
| Modify | `src/components/workspace-conversation-pane.ts` | 新增 `hasMore`/`onLoadMore` 属性 + 顶部加载触发 |

---

## Chunk 1: 后端 - Redis 索引写入 + 查询层

### Task 1: Enqueue 追加 workspace 索引写入

**Files:**
- Modify: `internal/buffer/redis_buffer.go:98-135`

- [ ] **Step 1: 在 `redis_buffer.go` 顶部常量组中添加 workspace 索引前缀**

在 `redisLastIndexPrefix` 后追加：
```go
redisWorkspaceProcessesPrefix = "workspace_processes:"
```

- [ ] **Step 2: 在 `Enqueue` 方法的 Pipeline 中追加 ZADD + EXPIRE**

在 `pipe.Expire(ctx, hashKey, rb.opts.TTL)` 之后（约第 108 行后），添加：
```go
// 维护 workspace 级 process 索引
wsZsetKey := redisWorkspaceProcessesPrefix + entry.WorkspaceID
pipe.ZAdd(ctx, wsZsetKey, &redis.Z{
    Score:  float64(entry.EntryTimestamp.UnixMilli()),
    Member: processID,
})
pipe.Expire(ctx, wsZsetKey, 7*24*time.Hour)
```

- [ ] **Step 3: 运行测试确认不破坏现有功能**

Run: `go test ./internal/buffer/ -v -count=1`
Expected: 14/14 PASS

- [ ] **Step 4: Commit**

```bash
git add internal/buffer/redis_buffer.go
git commit -m "feat: Enqueue 追加 workspace_processes ZSET 索引写入"
```

---

### Task 2: 创建 redis_reader.go - cursor 编解码 + Redis 查询

**Files:**
- Create: `internal/buffer/redis_reader.go`
- Create: `internal/buffer/redis_reader_test.go`

- [ ] **Step 1: 写 cursor 编解码的测试**

Create `internal/buffer/redis_reader_test.go`:
```go
package buffer

import (
	"testing"
)

func TestCursorEncodeDecode(t *testing.T) {
	original := cursor{TimestampMilli: 1743244800000, ProcessID: "proc-abc-123", EntryIndex: 42}
	encoded := encodeCursor(original)
	decoded, err := decodeCursor(encoded)
	if err != nil {
		t.Fatalf("decode failed: %v", err)
	}
	if decoded.TimestampMilli != original.TimestampMilli {
		t.Errorf("timestamp: got %d, want %d", decoded.TimestampMilli, original.TimestampMilli)
	}
	if decoded.ProcessID != original.ProcessID {
		t.Errorf("processID: got %s, want %s", decoded.ProcessID, original.ProcessID)
	}
	if decoded.EntryIndex != original.EntryIndex {
		t.Errorf("entryIndex: got %d, want %d", decoded.EntryIndex, original.EntryIndex)
	}
}

func TestDecodeEmptyCursor(t *testing.T) {
	_, err := decodeCursor("")
	if err == nil {
		t.Error("expected error for empty cursor")
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/buffer/ -run TestCursor -v -count=1`
Expected: FAIL（函数未定义）

- [ ] **Step 3: 实现 cursor 编解码**

Create `internal/buffer/redis_reader.go`:
```go
package buffer

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"sort"
	"strconv"
	"time"

	"github.com/go-redis/redis/v8"
	"github.com/huajiejun/kanban-watcher/internal/store"
)

// cursor 三段式分页游标
type cursor struct {
	TimestampMilli int64  `json:"ts"`
	ProcessID      string `json:"pid"`
	EntryIndex     int    `json:"ei"`
}

func encodeCursor(c cursor) string {
	data, _ := json.Marshal(c)
	return base64.RawURLEncoding.EncodeToString(data)
}

func decodeCursor(s string) (cursor, error) {
	if s == "" {
		return cursor{}, fmt.Errorf("empty cursor")
	}
	data, err := base64.RawURLEncoding.DecodeString(s)
	if err != nil {
		return cursor{}, fmt.Errorf("invalid cursor: %w", err)
	}
	var c cursor
	if err := json.Unmarshal(data, &c); err != nil {
		return cursor{}, fmt.Errorf("invalid cursor json: %w", err)
	}
	return c, nil
}

// RedisReader Redis 优先消息查询
type RedisReader struct {
	rdb *redis.Client
}

// NewRedisReader 创建 Redis 查询器
func NewRedisReader(rdb *redis.Client) *RedisReader {
	return &RedisReader{rdb: rdb}
}

// FetchWorkspaceMessages 从 Redis 查询工作区消息
// 返回消息列表、是否有更多数据、下一页 cursor、错误
func (rr *RedisReader) FetchWorkspaceMessages(ctx context.Context, workspaceID string, limit int, beforeCursor string) ([]store.ProcessEntry, bool, string, error) {
	if rr == nil || rr.rdb == nil {
		return nil, false, "", nil
	}

	wsZsetKey := redisWorkspaceProcessesPrefix + workspaceID

	// 解析 cursor
	var minScore float64
	if beforeCursor != "" {
		c, err := decodeCursor(beforeCursor)
		if err == nil {
			minScore = float64(c.TimestampMilli)
		}
	}

	// 1. 获取 processID 列表（按最新时间倒序）
	processIDs, err := rr.rdb.ZRevRange(ctx, wsZsetKey, 0, -1).Result()
	if err != nil {
		return nil, false, "", fmt.Errorf("ZREVRANGE workspace_processes: %w", err)
	}
	if len(processIDs) == 0 {
		return nil, false, "", nil
	}

	// 2. 逐个 process 读取消息
	var allEntries []store.ProcessEntry
	remaining := limit + 1 // 多读一条判断 has_more

	for _, processID := range processIDs {
		if remaining <= 0 {
			break
		}

		zsetKey := redisZSETKeyPrefix + processID
		hashKey := redisHashKeyPrefix + processID

		// 读取 ZSET members（按 score 倒序）
		var members []string
		if minScore > 0 {
			members, err = rr.rdb.ZRevRangeByScore(ctx, zsetKey, &redis.ZRangeBy{
				Min:   "-inf",
				Max:   fmt.Sprintf("%d", int64(minScore)-1),
				Count: int64(remaining),
			}).Result()
		} else {
			members, err = rr.rdb.ZRevRange(ctx, zsetKey, 0, int64(remaining-1)).Result()
		}
		if err != nil || len(members) == 0 {
			continue
		}

		// 从 Hash 获取数据
		vals, err := rr.rdb.HMGet(ctx, hashKey, toInterfaceSlice(members)...).Result()
		if err != nil {
			continue
		}

		for i, val := range vals {
			if val == nil {
				continue
			}
			s, ok := val.(string)
			if !ok {
				continue
			}
			var entry store.ProcessEntry
			if err := json.Unmarshal([]byte(s), &entry); err != nil {
				continue
			}
			allEntries = append(allEntries, entry)
			remaining--
			if remaining <= 0 {
				break
			}
		}
	}

	// 3. 按 entry_timestamp 倒序排列
	sort.Slice(allEntries, func(i, j int) bool {
		if allEntries[i].EntryTimestamp.Equal(allEntries[j].EntryTimestamp) {
			return allEntries[i].EntryIndex > allEntries[j].EntryIndex
		}
		return allEntries[i].EntryTimestamp.After(allEntries[j].EntryTimestamp)
	})

	// 4. 截取 limit + 1 判断 has_more
	hasMore := len(allEntries) > limit
	if hasMore {
		allEntries = allEntries[:limit]
	}

	// 5. 生成 cursor
	var nextCursor string
	if len(allEntries) > 0 {
		last := allEntries[len(allEntries)-1]
		nextCursor = encodeCursor(cursor{
			TimestampMilli: last.EntryTimestamp.UnixMilli(),
			ProcessID:      last.ProcessID,
			EntryIndex:     last.EntryIndex,
		})
	}

	return allEntries, hasMore, nextCursor, nil
}

// toInterfaceSlice 将 []string 转为 []interface{}（复用 redis_buffer.go 中的版本）
// 注意：redis_buffer.go 中已有此函数，但它是包级别的，可以直接使用
```

注意：文件末尾不要重复 `toInterfaceSlice`，因为 `redis_buffer.go` 中已存在包级函数。但 `HMGet` 需要的是 `[]string` 参数，需要确认 go-redis 的 HMGet 签名。检查后发现 `HMGet` 接受 `...string`，所以直接传 `members...` 即可。

修改 `FetchWorkspaceMessages` 中的 HMGet 调用：
```go
vals, err := rr.rdb.HMGet(ctx, hashKey, members...).Result()
```

并删除 `redis_reader.go` 中对 `toInterfaceSlice` 的注释引用。

- [ ] **Step 4: 运行测试**

Run: `go test ./internal/buffer/ -run TestCursor -v -count=1`
Expected: PASS

- [ ] **Step 5: 写 RedisReader 集成测试（需要 Redis 连接）**

在 `redis_reader_test.go` 追加：
```go
func TestRedisReaderFetchWorkspaceMessages(t *testing.T) {
	rdb := getTestRedisClient(t)
	defer rdb.Close()

	// 准备测试数据
	ctx := context.Background()
	processID := "test-proc-reader"
	workspaceID := "test-ws-reader"

	// 写入 workspace 索引
	rdb.ZAdd(ctx, "workspace_processes:"+workspaceID, &redis.Z{
		Score:  float64(time.Now().UnixMilli()),
		Member: processID,
	})

	// 写入消息
	for i := 1; i <= 5; i++ {
		entry := store.ProcessEntry{
			ProcessID:      processID,
			WorkspaceID:    workspaceID,
			EntryIndex:     i,
			EntryType:      "assistant_message",
			Role:           "assistant",
			Content:        "message " + strconv.Itoa(i),
			EntryTimestamp: time.Now().Add(time.Duration(i) * time.Minute),
		}
		data, _ := json.Marshal(entry)
		rdb.HSet(ctx, "process_entry_data:"+processID, strconv.Itoa(i), data)
		rdb.ZAdd(ctx, "process_entries:"+processID, &redis.Z{
			Score:  float64(i),
			Member: strconv.Itoa(i),
		})
	}

	// 测试查询
	reader := NewRedisReader(rdb)
	entries, hasMore, cursor, err := reader.FetchWorkspaceMessages(ctx, workspaceID, 3, "")
	if err != nil {
		t.Fatalf("FetchWorkspaceMessages failed: %v", err)
	}
	if len(entries) != 3 {
		t.Errorf("expected 3 entries, got %d", len(entries))
	}
	if !hasMore {
		t.Error("expected hasMore=true")
	}
	if cursor == "" {
		t.Error("expected non-empty cursor")
	}

	// 测试翻页
	entries2, hasMore2, _, err := reader.FetchWorkspaceMessages(ctx, workspaceID, 10, cursor)
	if err != nil {
		t.Fatalf("FetchWorkspaceMessages page2 failed: %v", err)
	}
	if len(entries2) != 2 {
		t.Errorf("expected 2 entries on page2, got %d", len(entries2))
	}
	if hasMore2 {
		t.Error("expected hasMore=false on page2")
	}

	// 清理
	rdb.Del(ctx, "workspace_processes:"+workspaceID, "process_entries:"+processID, "process_entry_data:"+processID)
}
```

注意：需要检查 `redis_reader_test.go` 是否能引用 `redis_buffer_test.go` 中已有的 `getTestRedisClient` 函数（同包可复用）。

- [ ] **Step 6: 运行全部 buffer 测试**

Run: `go test ./internal/buffer/ -v -count=1`
Expected: 全部 PASS

- [ ] **Step 7: Commit**

```bash
git add internal/buffer/redis_reader.go internal/buffer/redis_reader_test.go
git commit -m "feat: Redis 查询层 - cursor 分页 + 跨 process 消息读取"
```

---

## Chunk 2: 后端 - API 端点 + 路由注册

### Task 3: 新增 API handler 和响应类型

**Files:**
- Modify: `internal/api/messages.go`

- [ ] **Step 1: 添加新响应类型和 import**

在 `messages.go` 的 import 中添加：
```go
"encoding/base64"
"github.com/go-redis/redis/v8"
"github.com/huajiejun/kanban-watcher/internal/buffer"
```

在文件类型定义区域（约第 34 行后）添加：
```go
// WorkspaceMessagesResponse 工作区消息分页响应
type WorkspaceMessagesResponse struct {
	WorkspaceID string            `json:"workspace_id"`
	Messages    []MessageResponse `json:"messages"`
	HasMore     bool              `json:"has_more"`
	Cursor      string            `json:"cursor,omitempty"`
}
```

- [ ] **Step 2: 添加 HandleWorkspaceMessages handler**

在 `messages.go` 末尾（`reverseMessages` 函数前）添加：
```go
// HandleWorkspaceMessages 处理 GET /api/workspaces/{id}/messages
// Redis 优先 + MySQL 兜底的分页消息查询
func HandleWorkspaceMessages(w http.ResponseWriter, r *http.Request, dbStore *store.Store, rdb *redis.Client) {
	if r.Method != http.MethodGet {
		http.Error(w, "Method not allowed", http.StatusMethodNotAllowed)
		return
	}
	if dbStore == nil {
		http.Error(w, "数据库未初始化", http.StatusInternalServerError)
		return
	}

	path := strings.TrimPrefix(r.URL.Path, "/api/workspaces/")
	parts := strings.SplitN(path, "/", 2)
	if len(parts) < 1 || parts[0] == "" {
		http.Error(w, "Invalid path", http.StatusBadRequest)
		return
	}
	workspaceID := parts[0]

	// 解析参数
	limit := 50
	if raw := r.URL.Query().Get("limit"); raw != "" {
		if parsed, err := strconv.Atoi(raw); err == nil && parsed > 0 {
			if parsed > 200 {
				parsed = 200
			}
			limit = parsed
		}
	}
	beforeCursor := r.URL.Query().Get("before")

	// 获取 workspace 以确认 sessionID
	workspace, err := dbStore.GetWorkspaceByID(r.Context(), workspaceID)
	if err != nil {
		http.Error(w, "获取工作区失败", http.StatusInternalServerError)
		return
	}
	if workspace == nil || workspace.LatestSessionID == nil || *workspace.LatestSessionID == "" {
		writeJSON(w, WorkspaceMessagesResponse{WorkspaceID: workspaceID, Messages: []MessageResponse{}})
		return
	}
	sessionID := *workspace.LatestSessionID

	// Redis 优先查询
	var allEntries []store.ProcessEntry
	var hasMore bool
	var nextCursor string

	if rdb != nil {
		reader := buffer.NewRedisReader(rdb)
		redisEntries, redisHasMore, redisCursor, redisErr := reader.FetchWorkspaceMessages(
			r.Context(), workspaceID, limit, beforeCursor,
		)
		if redisErr != nil {
			fmt.Fprintf(os.Stderr, "Redis 查询失败，降级到 MySQL: %v\n", redisErr)
		} else if len(redisEntries) > 0 {
			allEntries = redisEntries
			hasMore = redisHasMore
			nextCursor = redisCursor

			// Redis 数据足够，不需要查 MySQL
			if len(redisEntries) >= limit {
				// 已够 limit 条
			} else if !redisHasMore {
				// Redis 数据不够且无更多，从 MySQL 补充
				remaining := limit - len(redisEntries)
				oldestTimestamp := redisEntries[len(redisEntries)-1].EntryTimestamp
				mysqlEntries, mysqlErr := dbStore.GetSessionMessages(
					r.Context(), sessionID, remaining+1, oldestTimestamp, nil,
				)
				if mysqlErr == nil && len(mysqlEntries) > 0 {
					mysqlHasMore := len(mysqlEntries) > remaining
					if mysqlHasMore {
						mysqlEntries = mysqlEntries[:remaining]
					}
					allEntries = append(allEntries, mysqlEntries...)
					if mysqlHasMore {
						hasMore = true
					}
				}
			}
		}
	}

	// Redis 无数据或不可用，直接查 MySQL
	if len(allEntries) == 0 {
		var before time.Time
		if beforeCursor != "" {
			if c, err := decodeCursorInternal(beforeCursor); err == nil {
				before = time.UnixMilli(c.TimestampMilli)
			}
		}
		entries, dbErr := dbStore.GetSessionMessages(r.Context(), sessionID, limit+1, before, nil)
		if dbErr != nil {
			http.Error(w, "获取消息失败", http.StatusInternalServerError)
			return
		}
		hasMore = len(entries) > limit
		if hasMore {
			entries = entries[:limit]
		}
		allEntries = entries
	}

	// 生成 cursor（如果还没有）
	if nextCursor == "" && len(allEntries) > 0 {
		last := allEntries[len(allEntries)-1]
		c := cursorInternal{
			TimestampMilli: last.EntryTimestamp.UnixMilli(),
			ProcessID:      last.ProcessID,
			EntryIndex:     last.EntryIndex,
		}
		data, _ := json.Marshal(c)
		nextCursor = base64.RawURLEncoding.EncodeToString(data)
	}

	reverseMessages(allEntries)

	resp := WorkspaceMessagesResponse{
		WorkspaceID: workspaceID,
		Messages:    make([]MessageResponse, 0, len(allEntries)),
		HasMore:     hasMore,
		Cursor:      nextCursor,
	}

	for _, entry := range allEntries {
		item := MessageResponse{
			ID:         entry.ID,
			SessionID:  entry.SessionID,
			ProcessID:  entry.ProcessID,
			EntryIndex: entry.EntryIndex,
			EntryType:  entry.EntryType,
			Role:       entry.Role,
			Content:    entry.Content,
			Timestamp:  entry.EntryTimestamp.Format(time.RFC3339Nano),
		}
		if info := buildToolInfo(entry); len(info) > 0 {
			item.ToolInfo = info
		}
		resp.Messages = append(resp.Messages, item)
	}

	writeJSON(w, resp)
}

// 内部 cursor 类型（与 buffer.cursor 解耦，避免循环引用）
type cursorInternal struct {
	TimestampMilli int64  `json:"ts"`
	ProcessID      string `json:"pid"`
	EntryIndex     int    `json:"ei"`
}

func decodeCursorInternal(s string) (cursorInternal, error) {
	if s == "" {
		return cursorInternal{}, fmt.Errorf("empty cursor")
	}
	data, err := base64.RawURLEncoding.DecodeString(s)
	if err != nil {
		return cursorInternal{}, fmt.Errorf("invalid cursor: %w", err)
	}
	var c cursorInternal
	if err := json.Unmarshal(data, &c); err != nil {
		return cursorInternal{}, fmt.Errorf("invalid cursor json: %w", err)
	}
	return c, nil
}
```

注意：还需要在 import 中添加 `"os"`（用于 `fmt.Fprintf(os.Stderr, ...)`）。检查现有 import 中是否已有 `"os"`——现有 import 没有，需要添加。同时 `"encoding/base64"` 也需要添加。

- [ ] **Step 3: 运行编译检查**

Run: `go build ./internal/api/`
Expected: 编译通过

- [ ] **Step 4: Commit**

```bash
git add internal/api/messages.go
git commit -m "feat: HandleWorkspaceMessages API - Redis 优先查询 + MySQL 兜底"
```

---

### Task 4: 注册路由 + 传递 Redis 客户端

**Files:**
- Modify: `internal/server/server.go`

- [ ] **Step 1: 添加 redis 字段到 Server struct**

在 `server.go` 的 `Server` struct 中（约第 39 行后）添加字段：
```go
rdb            *redis.Client
```

并在 import 中添加：
```go
"github.com/go-redis/redis/v8"
```

- [ ] **Step 2: 添加 SetRedisClient 方法**

在 `SetStore` 方法后添加：
```go
func (s *Server) SetRedisClient(rdb *redis.Client) {
	s.rdb = rdb
}
```

- [ ] **Step 3: 在 handleWorkspaces 中注册 /messages 路由**

在 `handleWorkspaces` 方法中（约第 914 行，`latest-messages` 路由之后）添加：
```go
// GET /api/workspaces/{id}/messages
if len(parts) == 2 && parts[1] == "messages" && r.Method == http.MethodGet {
    api.HandleWorkspaceMessages(w, r, s.store, s.rdb)
    return
}
```

- [ ] **Step 4: 运行编译检查**

Run: `go build ./internal/server/`
Expected: 编译通过

- [ ] **Step 5: Commit**

```bash
git add internal/server/server.go
git commit -m "feat: 注册 /api/workspaces/{id}/messages 路由 + 传递 Redis 客户端"
```

---

### Task 5: 在启动流程中注入 Redis 客户端

**Files:**
- Modify: `cmd/kanban-watcher/run.go`

- [ ] **Step 1: 在 `runDaemon` 和 `runHeadless` 中注入 Redis**

找到 `httpServer.SetStore(dbStore)` 调用位置，在其后添加：
```go
if redisCli != nil {
    httpServer.SetRedisClient(redisCli.RDB())
}
```

`redisCli` 变量已由 `initBuffer` 返回，直接使用即可。两个函数中都需要添加。

- [ ] **Step 2: 运行编译检查**

Run: `go build ./cmd/kanban-watcher/`
Expected: 编译通过

- [ ] **Step 3: 运行全部 Go 测试**

Run: `go test ./... -count=1 2>&1 | tail -20`
Expected: 全部 PASS

- [ ] **Step 4: Commit**

```bash
git add cmd/kanban-watcher/run.go
git commit -m "feat: 启动流程注入 Redis 客户端到 HTTP Server"
```

---

## Chunk 3: 前端 - API 调用 + 消息加载 + 分页

### Task 6: 新增 fetchWorkspaceMessages API 函数

**Files:**
- Modify: `src/lib/http-api.ts`

- [ ] **Step 1: 在 `fetchWorkspaceLatestMessages` 函数后添加新函数**

```typescript
export async function fetchWorkspaceMessages({
  baseUrl,
  apiKey,
  workspaceId,
  limit = 50,
  before,
}: RequestOptions & {
  workspaceId: string;
  limit?: number;
  before?: string;
}): Promise<{
  workspace_id: string;
  messages: SessionMessageResponse[];
  has_more: boolean;
  cursor: string;
}> {
  const query = new URLSearchParams({ limit: String(limit) });
  if (before) {
    query.set("before", before);
  }
  return fetchJSON(
    `${normalizeBaseUrl(baseUrl)}/api/workspaces/${workspaceId}/messages?${query.toString()}`,
    {
      method: "GET",
      headers: buildHeaders(apiKey),
    },
  );
}
```

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `npx tsc --noEmit --pretty 2>&1 | head -20`
Expected: 无错误

- [ ] **Step 3: Commit**

```bash
git add src/lib/http-api.ts
git commit -m "feat: fetchWorkspaceMessages API 函数 - cursor 分页查询"
```

---

### Task 7: workspace-home.ts 改用新 API + loadMore

**Files:**
- Modify: `src/web/workspace-home.ts`

- [ ] **Step 1: 添加 import 和状态属性**

在 import 区添加 `fetchWorkspaceMessages`（替换或追加到已有的 `fetchWorkspaceLatestMessages` import）。

在 static properties 中添加：
```typescript
cursorByWorkspace: { attribute: false },
hasMoreByWorkspace: { attribute: false },
```

在实例属性区初始化：
```typescript
cursorByWorkspace: Record<string, string> = {};
hasMoreByWorkspace: Record<string, boolean> = {};
```

- [ ] **Step 2: 修改 `loadWorkspaceMessages` 方法**

将方法体中 API 模式下的调用从 `fetchWorkspaceLatestMessages` 改为 `fetchWorkspaceMessages`：

```typescript
const response = await fetchWorkspaceMessages({
  baseUrl: this.previewOptions.baseUrl!,
  apiKey: this.previewOptions.apiKey,
  workspaceId,
  limit: this.previewOptions.messagesLimit ?? 50,
});
```

在设置 `messagesByWorkspace` 后，保存 cursor 和 hasMore：
```typescript
this.cursorByWorkspace = {
  ...this.cursorByWorkspace,
  [workspaceId]: response.cursor ?? "",
};
this.hasMoreByWorkspace = {
  ...this.hasMoreByWorkspace,
  [workspaceId]: response.has_more ?? false,
};
```

注意：`normalizeApiMessages` 的输入类型需要兼容。当前 `normalizeApiMessages` 接受 `SessionMessageResponse[]`，新 API 返回的 `messages` 字段也是相同结构，无需修改。

- [ ] **Step 3: 添加 `loadMoreMessages` 方法**

```typescript
private async loadMoreMessages(workspaceId: string) {
  if (!this.hasMoreByWorkspace[workspaceId]) {
    return;
  }
  const cursor = this.cursorByWorkspace[workspaceId];
  if (!cursor) {
    return;
  }

  try {
    const response = await fetchWorkspaceMessages({
      baseUrl: this.previewOptions.baseUrl!,
      apiKey: this.previewOptions.apiKey,
      workspaceId,
      limit: 50,
      before: cursor,
    });

    const olderMessages = normalizeApiMessages(response.messages);
    const currentMessages = this.messagesByWorkspace[workspaceId] ?? [];

    // 去重：合并时排除重复消息
    const existingKeys = new Set(currentMessages.map((m) => getDialogMessageIdentity(m)));
    const newMessages = olderMessages.filter((m) => !existingKeys.has(getDialogMessageIdentity(m)));

    this.messagesByWorkspace = {
      ...this.messagesByWorkspace,
      [workspaceId]: [...currentMessages, ...newMessages],
    };
    this.cursorByWorkspace = {
      ...this.cursorByWorkspace,
      [workspaceId]: response.cursor ?? "",
    };
    this.hasMoreByWorkspace = {
      ...this.hasMoreByWorkspace,
      [workspaceId]: response.has_more ?? false,
    };
  } catch (error) {
    console.error("加载更多消息失败:", error);
  }
}
```

注意：`getDialogMessageIdentity` 已在 `dialog-messages.ts` 中导出，确认 import。

- [ ] **Step 4: 暴露 loadMore 给 conversation-pane**

找到渲染 `workspace-conversation-pane` 的模板位置，添加属性：
```typescript
.hasMore=${this.hasMoreByWorkspace[workspaceId] ?? false}
@load-more=${() => this.loadMoreMessages(workspaceId)}
```

- [ ] **Step 5: 验证 TypeScript 编译**

Run: `npx tsc --noEmit --pretty 2>&1 | head -20`
Expected: 无错误

- [ ] **Step 6: Commit**

```bash
git add src/web/workspace-home.ts
git commit -m "feat: workspace-home 改用 cursor 分页 API + loadMoreMessages"
```

---

### Task 8: conversation-pane 添加顶部加载触发

**Files:**
- Modify: `src/components/workspace-conversation-pane.ts`

- [ ] **Step 1: 添加 hasMore 属性**

在 `static properties` 中添加：
```typescript
hasMore: { type: Boolean },
```

在实例属性中初始化：
```typescript
hasMore = false;
```

- [ ] **Step 2: 修改 `handleMessageListScroll` 添加顶部检测**

在现有 `handleMessageListScroll` 方法中，添加滚动到顶部的检测逻辑：
```typescript
private handleMessageListScroll = () => {
  const messageList = this.shadowRoot?.querySelector(".message-list") as HTMLDivElement | null;
  if (!messageList) {
    return;
  }

  // 检测是否滚动到顶部（加载更早消息）
  if (messageList.scrollTop <= 8 && this.hasMore) {
    this.dispatchEvent(new CustomEvent("load-more", { bubbles: true, composed: true }));
  }

  // 检测是否滚动到底部（自动滚动标记）
  const distanceToBottom = messageList.scrollHeight - messageList.clientHeight - messageList.scrollTop;
  this.shouldAutoScroll = distanceToBottom <= 8;
};
```

- [ ] **Step 3: 验证 TypeScript 编译**

Run: `npx tsc --noEmit --pretty 2>&1 | head -20`
Expected: 无错误

- [ ] **Step 4: Commit**

```bash
git add src/components/workspace-conversation-pane.ts
git commit -m "feat: conversation-pane 滚动到顶部触发 load-more 事件"
```

---

## Chunk 4: 测试验证

### Task 9: 手动测试验证

- [ ] **Step 1: 重启后端**

Run: `./scripts/start-dev.sh restart` 或手动重启
Expected: 服务启动，日志无报错

- [ ] **Step 2: 验证 workspace_processes ZSET 写入**

Run: `redis-cli ZRANGE workspace_processes:{某个workspaceID} 0 -1 WITHSCORES`
Expected: 能看到 processID 和时间戳

- [ ] **Step 3: 验证新 API 端点**

Run: `curl http://localhost:7778/api/workspaces/{id}/messages?limit=10`
Expected: 返回 JSON 包含 messages、has_more、cursor

- [ ] **Step 4: 验证分页**

使用上一步返回的 cursor：
Run: `curl http://localhost:7778/api/workspaces/{id}/messages?limit=10&before={cursor}`
Expected: 返回更早的消息

- [ ] **Step 5: 验证前端加载**

打开浏览器，点击工作区卡片，确认消息加载正常。滚动到顶部观察是否加载更多。

- [ ] **Step 6: 最终 Commit**

如有调整，统一提交：
```bash
git add -A
git commit -m "fix: 测试验证后的调整"
```
