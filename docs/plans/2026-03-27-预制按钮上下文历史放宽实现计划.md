# 预制按钮上下文历史放宽 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将快捷按钮的 LLM 分析上下文改为直接使用最后 3 条消息，并在拼接文本中标记顺序。

**Architecture:** 调整上下文构建函数与两处调用侧消息采样逻辑，统一传入最近 3 条文本消息，不限角色。上下文最终以带顺序的纯文本发送给 LLM。

**Tech Stack:** TypeScript, Lit, Vitest

---

## Chunk 1: 测试先行

### Task 1: 为上下文拼接新增失败测试

**Files:**
- Modify: `tests/quick-buttons-llm.test.ts`
- Test: `tests/quick-buttons-llm.test.ts`

- [ ] **Step 1: 写失败测试**
- [ ] **Step 2: 运行测试确认失败**
- [ ] **Step 3: 写最小实现**
- [ ] **Step 4: 运行测试确认通过**
