# 手机端预制按钮收缩图标化 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将手机端预制按钮的展开入口从单独一行文字按钮改成同一行右侧的图标按钮。

**Architecture:** 复用现有收缩状态逻辑，仅调整 `workspace-conversation-pane` 的快捷按钮容器布局、切换按钮内容与样式。

**Tech Stack:** TypeScript, Lit, Vitest

---

## Chunk 1: 图标化切换入口

### Task 1: 更新测试与实现

**Files:**
- Modify: `src/components/workspace-conversation-pane.ts`
- Modify: `src/styles.ts`
- Modify: `tests/workspace-conversation-pane.test.ts`

- [ ] **Step 1: 写失败测试**
- [ ] **Step 2: 运行测试确认失败**
- [ ] **Step 3: 写最小实现**
- [ ] **Step 4: 运行测试确认通过**
