# 启动脚本日志跟随命令 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `scripts/start-dev.sh` 增加 `logs` 子命令，让服务后台启动后可以单独持续跟随前后端日志。

**Architecture:** 保持 `start` / `stop` / `status` 现有职责不变，只新增一个 `logs` 入口统一执行 `tail -F`；先用脚本级测试验证 `logs` 会跟踪正确的后端与前端日志文件，再补最小实现。

**Tech Stack:** Bash, shell test, `tail -F`

---

## Chunk 1: 测试先行

### Task 1: 补 logs 子命令失败测试

**Files:**
- Create: `scripts/test_start_dev_logs_command.sh`
- Modify: `scripts/start-dev.sh`

- [ ] **Step 1: 写失败测试**

通过 fake `tail` 截获 `./scripts/start-dev.sh logs <worktree_id>` 的调用参数，断言它会执行：

```bash
tail -F /tmp/kanban-backend-<port>.log /tmp/kanban-frontend-<port>.log
```

- [ ] **Step 2: 运行测试确认失败**

Run: `bash scripts/test_start_dev_logs_command.sh`
Expected: FAIL，提示脚本尚未支持 `logs` 命令

## Chunk 2: 最小实现

### Task 2: 增加 logs 子命令

**Files:**
- Modify: `scripts/start-dev.sh`

- [ ] **Step 1: 命令解析支持 `logs`**
- [ ] **Step 2: 新增 `show_logs` 函数**
- [ ] **Step 3: 更新帮助信息**

## Chunk 3: 验证

### Task 3: 验证 logs 行为

**Files:**
- Verify: `scripts/test_start_dev_logs_command.sh`

- [ ] **Step 1: 运行脚本测试**

Run: `bash scripts/test_start_dev_logs_command.sh`
Expected: PASS
